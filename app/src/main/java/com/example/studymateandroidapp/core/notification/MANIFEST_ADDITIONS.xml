<!-- 
    Required AndroidManifest.xml additions for the notification system.
    Merge these into your existing AndroidManifest.xml.
-->

<!-- ═══ Permissions ═══════════════════════════════════════ -->

<!-- Required for AlarmManager exact alarms on API 31+ -->
<uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

<!-- Required for posting notifications on API 33+ -->
<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

<!-- Required for BootReceiver to reschedule alarms after reboot -->
<uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

<!-- Required for AlarmManager to fire during Doze mode -->
<uses-permission android:name="android.permission.WAKE_LOCK" />


<!-- ═══ Within <application> tag ═════════════════════════ -->

<!-- Alarm-triggered reminder receiver -->
<receiver
    android:name=".core.notification.ReminderReceiver"
    android:exported="false" />

<!-- Boot receiver to reschedule alarms after reboot -->
<receiver
    android:name=".core.notification.BootReceiver"
    android:exported="false">
    <intent-filter>
        <action android:name="android.intent.action.BOOT_COMPLETED" />
    </intent-filter>
</receiver>
